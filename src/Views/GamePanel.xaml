<UserControl x:Class="meGaton.Views.GamePanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="700" 
             >

    <StackPanel Orientation="Horizontal" Margin="10">
        <materialDesign:Card
            materialDesign:ShadowAssist.ShadowDepth="Depth3" 
            Padding="32" Height="140" Width="600">
            <materialDesign:Card.LayoutTransform>
                <ScaleTransform ScaleX="{Binding PanelSizes.MyScale.Value}" ScaleY="{Binding PanelSizes.MyScale.Value}"/>
            </materialDesign:Card.LayoutTransform>
            <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Image Grid.Row="0" Grid.Column="0" Height="100" Width="100" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="0,-10,0,-10" />
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding GameName}" TextAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap"  FontSize="32"/>
                <Button Grid.Row="0" Grid.Column="2" Content="Run" Command="{Binding OkCommand}"/>
            </Grid>
        </materialDesign:Card>
    </StackPanel>
</UserControl>
