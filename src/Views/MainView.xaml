<Window x:Class="meGaton.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        WindowState="Maximized" WindowStyle="None" Topmost="True"
        Title="meGaton" Height="1080" Width="1920">
    <Grid HorizontalAlignment="Left" Height="1080" Margin="0,0,0,-8" VerticalAlignment="Top" Width="1910">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="424*"/>
            <ColumnDefinition Width="259*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="1" Name="PanelParent" HorizontalAlignment="Left" Height="1200" VerticalAlignment="Top" Width="868" Margin="0,-62,-350,-370">
        </StackPanel>
    </Grid>
</Window>
