<Window x:Class="meGaton.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        WindowState="Maximized" WindowStyle="None" Topmost="True"
        Title="meGaton" Height="1080" Width="1980">
    <Grid HorizontalAlignment="Left" Height="1080" Margin="0,0,-8,-8" VerticalAlignment="Top" Width="1980">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="47*"/>
            <ColumnDefinition Width="504*"/>
            <ColumnDefinition Width="439*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="2" Name="PanelParent" HorizontalAlignment="Left" Height="1200" VerticalAlignment="Top" Width="868" Margin="0,-62,0,-58">
        </StackPanel>
    </Grid>
</Window>
