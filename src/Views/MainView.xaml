<Window x:Class="meGaton.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        WindowState="Maximized" WindowStyle="None" Topmost="True"
        Title="meGaton" Height="1080" Width="1920">
    <Window.InputBindings>
        <KeyBinding Command="{Binding MyCommand}" Gesture="Ctrl+D" />
    </Window.InputBindings>
    <Grid HorizontalAlignment="Left" Height="1080" Margin="0,0,0,-8" VerticalAlignment="Top" Width="1920">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="1" Name="PanelParent" HorizontalAlignment="Left" Height="1243" VerticalAlignment="Top" Width="758" Margin="10,-91,0,-72">
        </StackPanel>
    </Grid>
</Window>
